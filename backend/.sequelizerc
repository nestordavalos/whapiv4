const { resolve } = require("path");

module.exports = {
  "config": resolve(__dirname, "dist", "src", "config", "database.js"),
  "modules-path": resolve(__dirname, "dist", "src", "models"),
  "migrations-path": resolve(__dirname, "dist", "src", "database", "migrations"),
  "seeders-path": resolve(__dirname, "dist", "src", "database", "seeds")
};
